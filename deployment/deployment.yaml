apiVersion: apps/v1
kind: Deployment
metadata:
  name: batts
  labels:
    app: batts
spec:
  replicas: 1
  selector:
    matchLabels:
      app: batts
  template:
    metadata:
      labels:
        app: batts
      annotations:
        instrumentation.opentelemetry.io/inject-sdk: "true"
    spec:
      containers:
        - name: batts-backend
          image: ghcr.io/dcnick3/batts:backend-52c270961facda72ab8c70dfa431b6d2aa1289ec
          env:
            - name: CONFIG_SERVER__ENDPOINT
              value: "127.0.0.1:3001"
            - name: CONFIG_AUTH__TELEGRAM_SECRET
              valueFrom:
                secretKeyRef:
                  name: batts-secrets
                  key: telegram_secret
        - name: batts-frontend
          image: ghcr.io/dcnick3/batts:frontend-52c270961facda72ab8c70dfa431b6d2aa1289ec
          env:
            - name: BACKEND_URL
              value: "http://127.0.0.1:3001"
          ports:
            - containerPort: 3000
